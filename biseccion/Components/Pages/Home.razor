@page "/"

<div style="padding-top: 6rem;">
    <h1>Método de Bisección</h1>
</div>

<p class="intro">
    El método de bisección es una técnica numérica para encontrar raíces de funciones continuas.  
    Consiste en tomar un intervalo inicial <code>[a, b]</code> donde la función cambia de signo (es decir, <code>f(a)·f(b) &lt; 0</code>),  
    y dividirlo repetidamente en mitades, seleccionando el subintervalo donde la raíz está ubicada.  
    Este proceso se repite hasta que la raíz se aproxima con la precisión deseada.
</p>

<h2>Características principales</h2>
<ul class="features-list">
    <li>Es un método simple y robusto.</li>
    <li>Siempre converge si la función es continua y se cumple el criterio de cambio de signo.</li>
    <li>La convergencia es lineal, por lo que puede ser más lento que otros métodos.</li>
    <li>Ideal para obtener una aproximación inicial confiable de la raíz.</li>
</ul>

<h2>Pasos del método</h2>
<ol class="steps-list">
    <li>Elegir el intervalo inicial <code>[a, b]</code> con <code>f(a)·f(b) &lt; 0</code>.</li>
    <li>Calcular el punto medio <code>c = (a + b)/2</code>.</li>
    <li>Evaluar <code>f(c)</code>. Si <code>f(c) = 0</code> o la precisión es suficiente, detener.</li>
    <li>Determinar el subintervalo <code>[a, c]</code> o <code>[c, b]</code> donde la función cambia de signo.</li>
    <li>Repetir el proceso con el nuevo intervalo.</li>
</ol>

<h2>Aplicaciones</h2>
<p class="applications">
    El método se usa ampliamente en problemas donde se requiere encontrar raíces reales de ecuaciones no lineales,  
    especialmente cuando no se conoce una fórmula explícita o cuando se quiere garantizar la convergencia.
</p>

<style>
    h1 {
        color: #2c3e50;
        font-weight: 700;
        margin-bottom: 1rem;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    p.intro, p.applications {
        font-size: 1.1rem;
        line-height: 1.6;
        color: #34495e;
        max-width: 700px;
        margin-bottom: 1.5rem;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    code {
        background-color: #f5f5f5;
        padding: 0.2rem 0.4rem;
        border-radius: 3px;
        font-family: Consolas, 'Courier New', monospace;
        font-size: 0.95rem;
    }

    h2 {
        color: #2980b9;
        margin-top: 2rem;
        margin-bottom: 1rem;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    ul.features-list, ol.steps-list {
        max-width: 700px;
        padding-left: 1.2rem;
        color: #2c3e50;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        font-size: 1rem;
        margin-bottom: 1.5rem;
    }

    ul.features-list li, ol.steps-list li {
        margin-bottom: 0.5rem;
    }
</style>
